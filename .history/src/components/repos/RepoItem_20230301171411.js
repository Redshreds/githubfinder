import React from "react";
import PropTypes from "prop-types";



export const RepoItem = ({ repo }) => {
  return <div className="card">RepoItem
  
    </div>;
};

RepoItem.propTypes ={
    repo: PropTypes.object.isRequired;
    
};

export default RepoItem;